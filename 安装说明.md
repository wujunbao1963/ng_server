# iOS 推送功能更新包使用说明

## 📦 包文件

**文件名**: `ios-push-refactor-phase1.tar.gz`  
**大小**: 109 KB  
**适用分支**: `refactor/phase1-foundation`

---

## 🚀 快速安装步骤

### 1. 备份当前代码（重要！）

在更新之前，先备份您的当前代码：

```bash
# 在您的项目目录
cd /path/to/ng_server

# 确认在正确的分支
git status

# 提交所有未提交的改动
git add .
git commit -m "Backup before iOS push update"

# 或者创建一个备份分支
git checkout -b backup-before-ios-push
git checkout refactor/phase1-foundation
```

### 2. 下载并解压更新包

```bash
# 在您的项目根目录
cd /path/to/ng_server

# 解压更新包（会覆盖现有文件）
tar -xzf ios-push-refactor-phase1.tar.gz

# 查看解压的文件
tar -tzf ios-push-refactor-phase1.tar.gz
```

**解压的文件**：
```
src/infra/ports/apns-push-provider.ts          （新文件）
src/infra/ports/multi-push-provider.ts         （新文件）
src/infra/ports/index.ts                       （修改）
src/infra/infra.module.ts                      （修改）
src/common/outbox/push-notification.handler.ts （修改）
package.json                                    （修改）
package-lock.json                               （修改）
.env                                            （修改）
.gitignore                                      （新文件）
README.md                                       （修改）
DEPLOY.md                                       （新文件）
docs/IOS_PUSH_SETUP.md                         （新文件）
docs/IOS_PUSH_SUMMARY_CN.md                    （新文件）
```

### 3. 安装新的依赖

```bash
# 安装 iOS 推送所需的依赖
npm install

# 验证安装
npm list @parse/node-apn
```

### 4. 构建项目

```bash
# 构建项目
npm run build

# 如果构建成功，说明代码更新正确
```

### 5. 验证更新

```bash
# 检查新文件是否存在
ls -la src/infra/ports/apns-push-provider.ts
ls -la src/infra/ports/multi-push-provider.ts

# 查看文档
cat docs/IOS_PUSH_SUMMARY_CN.md
```

### 6. 提交改动到 Git

```bash
# 查看所有改动
git status
git diff

# 添加所有改动
git add .

# 提交
git commit -m "Add iOS native push (APNs) support

- Add APNsPushProvider for iOS native notifications
- Add MultiPushProvider for platform-aware routing
- Update push notification handler
- Add comprehensive documentation
- Update dependencies"

# 推送到远程
git push origin refactor/phase1-foundation
```

---

## 📋 更新内容说明

### 新增功能

1. **iOS 原生推送 (APNs)**
   - 支持 iOS 设备的原生推送通知
   - 使用 Apple Push Notification service

2. **多渠道推送系统**
   - 自动根据设备平台路由（web/iOS）
   - Web Push 和 iOS Push 可同时工作

3. **平台感知发送**
   - 智能批量发送，按平台分组
   - 提高发送效率

### 新增文件

- `src/infra/ports/apns-push-provider.ts` - iOS 推送提供商（168 行）
- `src/infra/ports/multi-push-provider.ts` - 多渠道路由器（177 行）
- `docs/IOS_PUSH_SETUP.md` - 英文详细配置指南（290 行）
- `docs/IOS_PUSH_SUMMARY_CN.md` - 中文功能说明（188 行）
- `DEPLOY.md` - 部署指南（88 行）
- `.gitignore` - Git 忽略规则

### 修改的文件

- `src/infra/infra.module.ts` - 配置多提供商系统
- `src/infra/ports/index.ts` - 导出新的提供商
- `src/common/outbox/push-notification.handler.ts` - 使用平台感知发送
- `package.json` - 添加 @parse/node-apn 依赖
- `.env` - 添加 APNs 配置选项（注释形式）
- `README.md` - 添加 iOS 推送功能说明

### 新增依赖

- `@parse/node-apn` - Apple Push Notification 客户端库

---

## ⚙️ 配置 iOS 推送

更新后，如果需要启用 iOS 推送功能，需要在 Railway 或本地环境中配置：

```bash
APNS_KEY_ID=你的密钥ID
APNS_TEAM_ID=你的团队ID
APNS_KEY_CONTENT=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
APNS_BUNDLE_ID=com.neighbor-guard.app
APNS_PRODUCTION=false
```

详细配置步骤请查看：
- 英文：`docs/IOS_PUSH_SETUP.md`
- 中文：`docs/IOS_PUSH_SUMMARY_CN.md`

---

## 🔄 部署到 Railway

更新代码后，推送到 GitHub，然后：

1. 登录 Railway Dashboard
2. 选择项目 → 手动点击 "Deploy"
3. 选择分支：`refactor/phase1-foundation`
4. 添加 APNs 环境变量（如果需要 iOS 推送）
5. 等待部署完成

详细部署步骤请查看 `DEPLOY.md`

---

## 🔧 故障排查

### 解压失败

```bash
# 确保在项目根目录
pwd

# 检查包文件是否完整
ls -lh ios-push-refactor-phase1.tar.gz

# 测试解压（不实际解压）
tar -tzf ios-push-refactor-phase1.tar.gz
```

### 构建失败

```bash
# 删除 node_modules 重新安装
rm -rf node_modules
npm install

# 清理构建缓存
rm -rf dist
npm run build
```

### 依赖安装失败

```bash
# 使用 npm 清理缓存
npm cache clean --force
npm install

# 或使用 cnpm（中国镜像）
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### Git 冲突

如果解压后有 Git 冲突：

```bash
# 查看冲突文件
git status

# 对于每个冲突文件，选择保留新版本
git checkout --theirs <文件名>

# 或者放弃所有本地改动，使用新版本
git checkout .
tar -xzf ios-push-refactor-phase1.tar.gz
```

---

## 📞 获取帮助

如果遇到问题：

1. 查看 `docs/IOS_PUSH_SUMMARY_CN.md` 了解功能
2. 查看 `docs/IOS_PUSH_SETUP.md` 了解配置
3. 查看 `DEPLOY.md` 了解部署
4. 检查构建日志中的错误信息

---

## ✅ 验证清单

更新完成后，请确认：

- [ ] `npm install` 成功
- [ ] `npm run build` 成功
- [ ] 新文件存在：`src/infra/ports/apns-push-provider.ts`
- [ ] 新文件存在：`src/infra/ports/multi-push-provider.ts`
- [ ] 文档存在：`docs/IOS_PUSH_SETUP.md`
- [ ] 文档存在：`docs/IOS_PUSH_SUMMARY_CN.md`
- [ ] 文档存在：`DEPLOY.md`
- [ ] 代码已提交到 Git
- [ ] 代码已推送到 GitHub
- [ ] （可选）已部署到 Railway

---

**祝您更新顺利！** 🚀

如有问题，请参考文档或联系技术支持。
